import 'package:flutter/material.dart';

class EarningTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Earning')),
    );
  }
}
